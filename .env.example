# ============================================================================
# SYNTHORA - Environment Variables Template
# ============================================================================
# IMPORTANT: Copy this file to the project root as ".env.example"
# Then copy .env.example to .env and fill in the values
# NEVER commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# APP CONFIGURATION
# ----------------------------------------------------------------------------
APP_NAME=Synthora
APP_ENV=development  # development, staging, production
DEBUG=true  # Set to false in production
SECRET_KEY=your-secret-key-min-32-characters-long-change-this

# Backend URL (used for CORS, callbacks, etc.)
BACKEND_URL=http://localhost:8000
# Frontend URL
FRONTEND_URL=http://localhost:5173

# ----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# ----------------------------------------------------------------------------
# Railway provides this automatically as DATABASE_URL
# For local development, use your local PostgreSQL instance
DATABASE_URL=postgresql://postgres:password@localhost:5432/synthora

# Database encryption key for pgcrypto (generate a strong random key)
DB_ENCRYPTION_KEY=your-database-encryption-key-change-this

# ----------------------------------------------------------------------------
# REDIS (Upstash)
# ----------------------------------------------------------------------------
# Get this from Upstash dashboard after creating a Redis database
REDIS_URL=redis://default:your-password@your-endpoint.upstash.io:6379

# ----------------------------------------------------------------------------
# FIREBASE AUTHENTICATION
# ----------------------------------------------------------------------------
# Get these from Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_WEB_API_KEY=your-firebase-web-api-key

# Firebase Admin SDK (for backend)
# Option 1: Path to service account JSON file
FIREBASE_SERVICE_ACCOUNT_PATH=path/to/serviceAccountKey.json

# Option 2: Service account JSON as string (for Railway - set in dashboard)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Firebase Auth Domain (for frontend)
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com

# ----------------------------------------------------------------------------
# STRIPE (Payments)
# ----------------------------------------------------------------------------
# Get these from Stripe Dashboard > Developers > API Keys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Stripe Webhook Secret (from Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret

# Stripe Price IDs (create products in Stripe Dashboard)
STRIPE_PRICE_MONTHLY=price_monthly_id
STRIPE_PRICE_ANNUAL=price_annual_id

# ----------------------------------------------------------------------------
# GOOGLE CLOUD STORAGE
# ----------------------------------------------------------------------------
# Get these from Google Cloud Console > IAM & Admin > Service Accounts
GCS_BUCKET_NAME=synthora-videos
GCS_PROJECT_ID=your-gcp-project-id

# Option 1: Path to service account JSON file
GCS_SERVICE_ACCOUNT_PATH=path/to/gcs-serviceAccountKey.json

# Option 2: Service account JSON as string (for Railway - set in dashboard)
# GCS_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# ----------------------------------------------------------------------------
# CORS CONFIGURATION
# ----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# For production, set to your actual frontend domain
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ----------------------------------------------------------------------------
# RATE LIMITING
# ----------------------------------------------------------------------------
RATE_LIMIT_REQUESTS_PER_MINUTE=100

# ----------------------------------------------------------------------------
# AI INTEGRATIONS - API KEYS
# ----------------------------------------------------------------------------
# Note: These are OPTIONAL system-level defaults
# Users will configure their own API keys in the app
# Only set these if you want to provide fallback/default keys

# OpenAI (ChatGPT, Sora)
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key

# ElevenLabs (Voice AI)
# Get from: https://elevenlabs.io/app/settings/api-keys
# ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Pexels (Stock Media)
# Get from: https://www.pexels.com/api/
# PEXELS_API_KEY=your-pexels-api-key

# Unsplash (Stock Media)
# Get from: https://unsplash.com/developers
# UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# Google Veo 3 (Video AI)
# Get from: https://cloud.google.com/video-intelligence
# GOOGLE_VEO_API_KEY=your-google-veo-api-key

# Runway Gen-4 (Video AI)
# Get from: https://runwayml.com/
# RUNWAY_API_KEY=your-runway-api-key

# ImagineArt AI (Video AI)
# Get from: https://www.imagine.art/
# IMAGINEART_API_KEY=your-imagineart-api-key

# PixVerse (Video AI)
# Get from: https://pixverse.ai/
# PIXVERSE_API_KEY=your-pixverse-api-key

# Seedance AI (Video AI)
# Get from: https://seedance.ai/
# SEEDANCE_API_KEY=your-seedance-api-key

# Wan2.6 (Video AI)
# Get from: Wan2.6 documentation
# WAN_API_KEY=your-wan-api-key

# Hailuo AI (Video AI)
# Get from: https://hailuoai.com/
# HAILUO_API_KEY=your-hailuo-api-key

# Luma Dream Machine (Video AI)
# Get from: https://lumalabs.ai/dream-machine
# LUMA_API_KEY=your-luma-api-key

# LTX-2 (Video AI)
# Get from: LTX documentation
# LTX_API_KEY=your-ltx-api-key

# Creatomate (Video Assembly)
# Get from: https://creatomate.com/
# CREATOMATE_API_KEY=your-creatomate-api-key

# Shotstack (Video Assembly)
# Get from: https://shotstack.io/
# SHOTSTACK_API_KEY=your-shotstack-api-key

# Editframe (Video Assembly)
# Get from: https://editframe.com/
# EDITFRAME_API_KEY=your-editframe-api-key

# ----------------------------------------------------------------------------
# SOCIAL MEDIA OAUTH
# ----------------------------------------------------------------------------
# These are required for social media posting features

# YouTube (Google OAuth)
# Get from: https://console.cloud.google.com/apis/credentials
YOUTUBE_CLIENT_ID=your-youtube-client-id
YOUTUBE_CLIENT_SECRET=your-youtube-client-secret
YOUTUBE_REDIRECT_URI=${BACKEND_URL}/api/v1/social-accounts/callback/youtube

# TikTok
# Get from: https://developers.tiktok.com/
TIKTOK_CLIENT_KEY=your-tiktok-client-key
TIKTOK_CLIENT_SECRET=your-tiktok-client-secret
TIKTOK_REDIRECT_URI=${BACKEND_URL}/api/v1/social-accounts/callback/tiktok

# Instagram / Facebook (Meta)
# Get from: https://developers.facebook.com/
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
INSTAGRAM_REDIRECT_URI=${BACKEND_URL}/api/v1/social-accounts/callback/instagram
FACEBOOK_REDIRECT_URI=${BACKEND_URL}/api/v1/social-accounts/callback/facebook

# ----------------------------------------------------------------------------
# ENCRYPTION
# ----------------------------------------------------------------------------
# Master encryption key for API keys and tokens (Fernet key)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-encryption-key-generate-this

# ----------------------------------------------------------------------------
# WORKER CONFIGURATION
# ----------------------------------------------------------------------------
# RQ Worker settings
RQ_QUEUE_NAME=synthora
RQ_WORKER_COUNT=2

# ----------------------------------------------------------------------------
# VIDEO GENERATION
# ----------------------------------------------------------------------------
# Maximum video duration in seconds
MAX_VIDEO_DURATION=300

# Video quality settings
DEFAULT_VIDEO_RESOLUTION=1080p
DEFAULT_VIDEO_BITRATE=5000k

# FFmpeg path (if not in system PATH)
# FFMPEG_PATH=/usr/bin/ffmpeg

# ----------------------------------------------------------------------------
# STORAGE SETTINGS
# ----------------------------------------------------------------------------
# Video retention for free users (days)
FREE_USER_VIDEO_RETENTION_DAYS=30

# Maximum file size for uploads (bytes)
MAX_UPLOAD_SIZE=524288000  # 500MB

# ----------------------------------------------------------------------------
# FEATURE FLAGS
# ----------------------------------------------------------------------------
# Enable/disable features
FEATURE_AI_SUGGESTIONS=true
FEATURE_SCHEDULING=true
FEATURE_ANALYTICS=true

# ----------------------------------------------------------------------------
# LOGGING
# ----------------------------------------------------------------------------
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=json  # json, text

# ----------------------------------------------------------------------------
# SENTRY (Error Tracking - Optional)
# ----------------------------------------------------------------------------
# SENTRY_DSN=https://your-sentry-dsn

# ============================================================================
# RAILWAY-SPECIFIC NOTES
# ============================================================================
# When deploying to Railway:
# 1. DATABASE_URL is automatically provided by Railway PostgreSQL plugin
# 2. PORT is automatically provided by Railway
# 3. Set all other variables in Railway dashboard > Variables
# 4. For JSON values (Firebase, GCS service accounts), paste the entire JSON
#    as the value (Railway handles multiline values)
# 5. BACKEND_URL and FRONTEND_URL should be your Railway-provided URLs:
#    - Backend: https://synthora-api.up.railway.app
#    - Frontend: https://synthora-web.up.railway.app
# ============================================================================

