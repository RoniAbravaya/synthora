# ============================================================================
# SYNTHORA - Test Environment Variables
# ============================================================================
# INSTRUCTIONS: Copy this file to ".env" in the backend folder:
#   copy env.test.txt .env
# ============================================================================

# APP CONFIGURATION
APP_NAME=Synthora
APP_ENV=development
DEBUG=true
SECRET_KEY=test-secret-key-for-development-only-32chars

# URLs
BACKEND_URL=http://localhost:8000
FRONTEND_URL=http://localhost:5173

# DATABASE (SQLite for testing)
DATABASE_URL=sqlite:///./test.db

# REDIS (mock for testing)
REDIS_URL=redis://localhost:6379

# FIREBASE (test values)
FIREBASE_PROJECT_ID=test-project-id
FIREBASE_WEB_API_KEY=test-firebase-api-key

# STRIPE (test values - use Stripe test keys for real testing)
STRIPE_SECRET_KEY=sk_test_fake_key_for_testing
STRIPE_PUBLISHABLE_KEY=pk_test_fake_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret
STRIPE_MONTHLY_PRICE_ID=price_test_monthly
STRIPE_ANNUAL_PRICE_ID=price_test_annual

# GOOGLE CLOUD STORAGE (test values)
GCS_BUCKET_NAME=test-bucket
GCS_PROJECT_ID=test-gcs-project

# ENCRYPTION KEY (Fernet key for testing)
ENCRYPTION_KEY=dGVzdC1lbmNyeXB0aW9uLWtleS0zMi1jaGFyYWN0ZXJz

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
